#ifndef SIGNALSMITH_FUNCTIONAL_STUB_H
#define SIGNALSMITH_FUNCTIONAL_STUB_H

namespace std {

template<typename T>
class function {
public:
    function() : m_ptr(nullptr) {}
    template<typename Func>
    function(Func f) : m_ptr((void*)1) {}
    operator bool() const { return m_ptr != nullptr; }
private:
    void *m_ptr;
};

}

#endif
