---
key: mem-03c8f4555db2f0f8-1674
ns: default
created: 1780075970044
updated: 1780075970044
---

## Consecutive-record beat-grid snap + hold-clears-LED (looper, commit follows 728df58)

THREE APC state/timing fixes:
(1) OFFBEAT RECORD START: deferred record latched only at full-phrase downbeat (loopMachine.cpp at_phrase_start = m_masterPhase % m_masterLoopBlocks == 0), so a press near a beat waited up to a whole 16-beat phrase and started on the wrong subdivision ('one forward'/offbeat). FIX: gridStep=(M>=16)?M/16:M (M encodes 16 beats), latch gate at_beat_grid = m_masterPhase % gridStep == 0, AND loopClip.cpp _startRecording snaps m_recordStartPhaseOffset to nearest gridStep multiple (floor; +gridStep if rem*2>=gridStep). Consecutive recordings now slice on quant multiples, latch within <=1 beat. First loop M==0 stays immediate+sample-true.
(2) HOLD LEAVES LED ON: long-hold sent CLEAR_LAYER which re-arms pending RECORD on empty/just-erased track when grid exists (loopMachine.cpp:440) => pad stayed yellow. FIX: hold now sends LOOP_COMMAND_ERASE_TRACK_BASE+n; ERASE_TRACK handler rewritten to clear m_track_pending[track]=NONE + clearClip all layers (running-count-correct, not raw init) + never arm. Single-track scoped (old handler did global stopImmediate). LED -> off.
(3) RECORD LED red appears promptly once latch is <=1 beat (was waiting full phrase in PENDING/yellow).
New :4445 TIME fields gridStep= latchPhase=. Host test scripts/test-grid-snap.cpp ALL PASS.

GOTCHA: hardware behavioral validation BLOCKED when USB audio source not streaming - cbwr freezes at 1 (Core1 startUpdate is USB-IN-driven). A/B proved via known-good kernel also stalling. Firmware exonerated by host test + gated-diff; re-validate on Pi once UCA222/APC audio connected.
