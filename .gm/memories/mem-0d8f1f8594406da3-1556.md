---
key: mem-0d8f1f8594406da3-1556
ns: default
created: 1781091855872
updated: 1781091855872
---

## Looper synced latch microrepeat (beat-repeat/stutter), notes 82-86 (commit follows)

patches/microRepeat.h: DSP stage in loopMachine::update on m_input_buffer AFTER pitch+effects, BEFORE cbWriteBlock. Holding note 82=1 beat, 83=1/2, 84=1/4, 85=1/8, 86=1/16 beat repeats a beat-fraction slice of the FULL mix (input + ALL loops) synced to the master grid (beat=m_masterLoopBlocks/16 blocks; slice=beat/div*AUDIO_BLOCK_SAMPLES, clamped MR_MAX_SLICE=48000). Latch = m_microRepeatDiv set in apcKey25::handleMidi note-on 82-86 BEFORE pad/button dispatch (note 84 OVERRIDES APC_BTN_FORMAT; note-off clears only if it owns the active div, newer held note wins), published LiveParams.microRepeatDiv (paramSnapshot), read Core 1. EPHEMERAL/position-passthrough (load-bearing): the stage NEVER touches clip m_play_block or m_masterPhase, so loops keep advancing underneath and on release the signal resumes EXACTLY where it would have been. Loop sum folded into m_input_buffer before the stage ((1-foldEnd)*wet remainder), final-mix dry loop add gated by (1-mrWet) so loop sum counts once; cbWriteBlock records the stutter -> under SHIFT it records INTO a recording loop. Click-free 1/16-per-block wet ramp; slice whole-sample (grid-aligned wrap). div==0 + settled = stage fully skipped, chain byte-identical (no regression to SHIFT/pause/recording). midiMap.h MA_MICROREPEAT (controller-agnostic). :4445 TIME microRep=<div>. Witnessed scripts/test-microrepeat.cpp ALL PASS (7 checks) + regression suite ALL PASS; clean build kernel7l.img 1062092B; flashed SD E:.
