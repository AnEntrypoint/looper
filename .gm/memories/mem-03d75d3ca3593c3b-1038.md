---
key: mem-03d75d3ca3593c3b-1038
ns: default
created: 1779712826269
updated: 1779712826269
---

## Resolved mutable: clicks-at-block-boundaries-drain

FOUND the real artifact via block-peak envelope (/tmp/wave): the -12 output amplitude OSCILLATES regularly between ~0.19 and ~0.79 (a ~12dB swing) with a ~384-sample / ~8ms / ~125Hz period. It's NOT clicks — it's a deep ~125Hz AMPLITUDE MODULATION = the buzzy gurgle. The 384-sample spacing matches the click-spacing cluster. 125Hz is unrelated to the 55Hz output or 110Hz input harmonically, and is independent of splice rate. Strong candidate: the two read heads (rdA active + rdB passive) are summed during crossfades, and if they are offset by a NON-integer period (splice-match slid the point by up to ±per/2, breaking exact integer-period alignment!) the two heads BEAT against each other at the offset frequency. The splice-match refinement I added (slides ±per/2) likely BROKE the integer-period phase coherence, turning the seamless overlap into a beating two-tone. Test: REVERT splice-match (the ±per/2 slide) so jumps stay exact integer periods, eliminating the beat.
