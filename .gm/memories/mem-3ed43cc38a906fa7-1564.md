---
key: mem-3ed43cc38a906fa7-1564
ns: default
created: 1780213766725
updated: 1780213766725
---

## Looper consecutive-loop unified quant grid (505-like, commit follows 6e709ca)

Symptom: first loop perfect, but consecutive loops 'fell apart / off by a quantized amount' after ~2. Root cause: the four quant touchpoints used INCONSISTENT references. Specifically the clip's AUDIO content-start (m_recStartBlock = raw backdated press) and its PHASE anchor (m_recordStartPhaseOffset = beat-snapped) were snapped INDEPENDENTLY, so clip block 0's audio != phase-zero by a sub-beat/division => drift that compounds across loops.

FIX (loopClip.cpp): ONE grid = powers-of-two of phrase M, anchored to masterPhase%M==0 (phrase 0). _startRecording applies the SAME beat-snap delta to BOTH m_recStartBlock and recordStartPhaseOffset (tied). _startEndingRecording FLOORS (not round) BOTH recordStartPhaseOffset AND m_recStartBlock by the same floorBack to the L-grid (grid=min(L,M)). So clip block 0 audio == grid instant == phase zero. _calcQuantizeTarget length stays {M/8..8M}; playback play_block=(masterPhase-offset)%L lands 0 at phrase boundaries for all L (sub-phrase L|M; multiple jM starts phase0). wasFirst still skipped (first loop defines grid, exact-region). Req1: phrase M stays RAW first-loop length (re-snapping M shifts first loop off its region); linkDeriveQuant only sets broadcast s_bpm to nearest-120. Host test scripts/test-consec-lock.cpp ALL PASS: L in {M/4,M/2,M,2M,4M} all phase-lock to phrase 0; near-boundary presses snap forgivingly. RULE: clip content-start and phase-anchor must ALWAYS move together (same delta) - never snap independently.
