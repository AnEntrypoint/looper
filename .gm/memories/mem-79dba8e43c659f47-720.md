---
key: mem-79dba8e43c659f47-720
ns: default
created: 1780744342401
updated: 1780744342401
---

## Resolved mutable: pause-update-gating

loopTrack.cpp:118-119 update() iterates ALL m_num_used_clips and calls m_clips[i]->update() UNCONDITIONALLY - not gated on running-count or track-state. So a paused clip is still updated and its head advances as long as its own m_state stays CS_PLAYING/CS_LOOPING (loopClipUpdate.cpp:174 advance runs when pp_main set, which is set in CS_PLAYING/CS_LOOPING). loopTrack.cpp:42-47 getTrackState reports PLAYING iff a clip is CS_PLAYING||CS_LOOPING, else STOPPED. CONCLUSION/DESIGN: paused clip stays CS_PLAYING (head advances) + new m_paused=true gates output; getTrackState must additionally treat m_paused as STOPPED so LED blink-yellow + paused-tap-resume gesture keep working.
