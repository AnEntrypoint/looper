---
key: mem-0f99e801b08c93e5-814
ns: default
created: 1782133224430
updated: 1782133224430
---

## Resolved mutable: output-update-not-running

LIVE DIAG: outUpd=0 (AudioOutputUSB::update NEVER entered) while cbwr climbs ~1515/s (looper.update runs). doUpdate's walk `for(p...) if(p->m_numConnections) p->update()` reaches+runs looper but SKIPS output -> output.m_numConnections==0 at the gate. The AudioConnection ctor does m_dest.m_numConnections++ so output (dest of 2 looper connections) should be 2; it reads 0 at the gate => the looper->output connections either weren't constructed or the gate is wrong for a pure SINK. FIX (robust, data-model-correct): a sink MUST drain its input queue every block, so doUpdate must call update() on output-type streams regardless of m_numConnections -- gate on `(p->m_numConnections || p->getType()==AUDIO_DEVICE_OUTPUT)`. That guarantees the output node always runs.
