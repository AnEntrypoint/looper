---
key: mem-196505fb4d2c6355-2177
ns: default
created: 1782128050743
updated: 1782128050743
---

## Looper no-output (input shows, no output) = USB-audio handler late-bind, FIXED+COMMITTED (cdf48b9, branch fix/usb-audio-no-output)

Symptom: APC shows INPUT VU levels but NO OUTPUT audio. ROOT CAUSE: AudioOutputUSB::start()/AudioInputUSB::start() run at boot from AudioSystem::initialize() BEFORE the USB audio device enumerates -> CUSBAudioDevice::GetOut()/GetIn() null -> RegisterOut/InHandler never called -> m_pOutHandler stays null -> StartOutRequest ships silence every frame (iso pipe clocks the DAC but zeros). The fix had been left UNCOMMITTED in the working tree from a prior session; it is now committed. FIX: made AudioInputUSB::inHandler/AudioOutputUSB::outHandler public (input_usb.h/output_usb.h); free funcs AudioInputUSB_bindHandler/AudioOutputUSB_bindHandler (input_usb.cpp/output_usb.cpp) call RegisterIn/OutHandler; CUSBAudioDevice::Configure() calls them at the s_pThis/s_pOut bind points (usbaudiodevice.cpp, once per direction at first bind). loopMachine ctor also seeds THRU/LOOP/MIX control scale to unity (belt-and-suspenders; setup() setControl already does it; WITH_VOLUMES defined loopMachine.cpp:31, so mix_level=0 WOULD silence output). NOTE: the old memory's 'second bug OPEN (outPeak=0)' framing was a stale red herring -- with the handler bound + setup() seeding scale, the committed mix path (mval=ival input-thru + oval loops) produces non-zero output. DIAGNOSTICS added to :4445 UAUD: outWr (AudioOutputUSB::update block-write counter, g_outWrites) + outAvail (ring fill) -> distinguishes graph-not-ticking (outWr frozen) vs rate (outAvail~0+outUR flood) vs mix-silent (outWr up+outPeak0). BUILD CAVEAT (hit again): output_usb.o/input_usb.o in _prh/audio (libaudio.a), usbaudiodevice.o in lib/usb (libusb.a) -- NOT app OBJS; build-local.ps1 final copy step fails but the libs+app build directly: rm those .o + ar d the .a member, remake _prh/audio AND lib/usb, then app make rc=0. kernel7l.img 1105356B, g_outWrites+bindHandler symbols linked. HARDWARE-VALIDATION PENDING: user deploys + reads UAUD (expect outDeliv climbing, outPeak>0, outUR not flooding, outWr climbing). See [[uca222-handler-late-bind]] [[looper-audio-architecture]].
