---
key: mem-e9401f2f85019dad-947
ns: default
created: 1779989889297
updated: 1779989889297
---

## Timing feature HARDWARE-VALIDATED (looper) — commit 1cbd670 + heap-fix 067f02e

Latency-backdate + 3-min continuous buffer + Link train-on-first-loop confirmed LIVE on the Pi via :4445 TIME verb. BOOT-CRASH GOTCHA (load-bearing): the continuous buffer MUST be HEAP-allocated (malloc in cbInit, called from loopMachine ctor) — a ~16MB STATIC array (g_cbBuffer) overflows the rPi4 AARCH=32 BSS and crashes BEFORE kernel setup (blank HDMI, no syslog, no ping, no :4445). Rule: always malloc large buffers like loopBuffer does, never a static array. Validation: TIME idle -> cbwr advances (buffer always-on); inject record on/off ~2.28s (MIDI pad note 2 via UDP:4446) -> started=1 then ended=1, backdate=64 clamped=0 (latency compensated), qbeats100=400 (4 beats=1 bar) bpm=108 (nearest-120 from loop length). 12/12 TIME replies over 18s, emerg=0, ping <1ms = stable. Probe :4445 with PowerShell UdpClient (bash /dev/udp gives false negatives).
