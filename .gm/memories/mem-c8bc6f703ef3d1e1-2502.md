---
key: mem-c8bc6f703ef3d1e1-2502
ns: default
created: 1783801172805
updated: 1783801172805
---

AIR192 comb TRUE ROOT CAUSE + NATIVE-RATE FIX (2026-07-11, commit 53854a6, supersedes the 43b6aa9 cubic-interpolator framing): The 26.382Hz FM comb on the AIR192-only round-trip was a DEPOSIT-RATE mismatch, not fundamentally an interpolator problem. patches/usbaudiodevice.cpp StartInRequest sized the IN-ring deposit (m_nInSubmitBytes) from a biased nominal `nomRate = m_uRate<<16 * 65500/65536 / 8000` = 47973.63 samples/sec, while the AIR192's TRUE crystal runs 48004.88/sec (live fbRate=393256). That 31.25 samp/sec deposit deficit = 1 sample slip / ~1536 samples, forcing the IN reader's fractional rate_step (input_usb.cpp update()) to continuously smear the deficit -- and that continuous fractional read IS the comb (measured 1819-sample period; the 1536-vs-1819 gap is IN-ADC-vs-OUT-DAC clock diff + IN_TARGET_LAG phase chasing). WHY UCA222/US-2x2 clean: UCA222 (UAC1) deposits via GetResultLength() = device's TRUE delivered count, no bias; US-2x2's rate lands close to the biased claim. AIR192 uniquely hit because its real rate diverges 31/sec from the biased claim. FIX: deposit at the device's OWN rate -- `nomRate = m_fbRate` (the explicit feedback endpoint's frames/microframe Q16.16; same crystal drives the AIR192 ADC+DAC), clamped to [390000,396000] with unbiased-nominal fallback when feedback not yet valid. deposit==device => rate_step collapses to exactly IN_FRAC_ONE=65536 (unity) => no fractional read runs => bit-exact native-rate passthrough => comb gone AT THE SOURCE. The 65500/65536 bias was obsolete scaffolding (guarded against over-reading stale buffer tail, already prevented by the shipped microframe-offset decode fix m_nInSamplesPerPkt). LIVE-WITNESSED conclusive: :4445 UAUD on the deployed 53854a6 build shows inRateStep=65536 across all samples (fractional read collapsed to integer), inAvail stable at 80 (was draining 79->48/6s pre-fix), inUR=0, inRS=1 static. The cubic Hermite (43b6aa9, input_usb.cpp + output_usb.cpp) stays as the hard-resync-path reconstruction safety net. Added inRateStep to UAUD (kernel_run.cpp:467) as the live unity-read witness. METHOD LESSON: the interpolator-quality fix (cubic) treated the symptom (-17.7dB comb) while the deposit-rate fix eliminates the mechanism (comb gone). The user's architectural directive 'run at the device's native hz, don't resample continuously' pointed straight at the disease. inRateStep=65536 live is a STRONGER witness than a WAV -- it proves the mechanism is eliminated, not just measured-smaller.
