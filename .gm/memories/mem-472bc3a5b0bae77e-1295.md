---
key: mem-472bc3a5b0bae77e-1295
ns: default
created: 1782127688805
updated: 1782127688805
---

## Resolved mutable: no-output-root-cause

git status: 6 UNCOMMITTED modified files (loopMachine.cpp, input_usb.{cpp,h}, output_usb.{cpp,h}, usbaudiodevice.cpp) = a prior session's late-bind fix never committed/deployed. git show HEAD:patches/output_usb.cpp | grep -c AudioOutputUSB_bindHandler = 0 (NOT in HEAD). The fix: handlers made public (input_usb.h/output_usb.h), free-func bind hooks AudioInputUSB_bindHandler/AudioOutputUSB_bindHandler (input_usb.cpp:80/output_usb.cpp:131), called in CUSBAudioDevice::Configure at the s_pThis/s_pOut bind points (usbaudiodevice.cpp:176,182). ROOT CAUSE of no-output = the OUT handler was never registered because AudioOutputUSB::start() ran at boot before the USB device enumerated (GetOut() null), so m_pOutHandler stayed null and StartOutRequest shipped silence -- exactly the memory uca222-handler-late-bind primary mechanism. The deployed kernel predates this fix. The loopMachine.cpp ctor control-scale seed is a secondary belt-and-suspenders (setup() already calls setControl(THRU/LOOP/MIX,63) so mix_level=1.0 at boot; WITH_VOLUMES IS defined loopMachine.cpp:31). Mix path with input flowing produces non-zero output once the handler is bound. FIX = commit + build + deploy the WIP; add UAUD diagnostics (outWr/outAvail) to confirm on hardware.
