---
key: mem-380b0732772dccb2-1396
ns: default
created: 1780231475779
updated: 1780231475779
---

## Resolved mutable: floor-shifts-phase-not-audio

CONFIRMED. loopClipUpdate.cpp: recording fills the CLIP BUFFER incrementally during CS_RECORDING/MAIN via rp=getBlock(m_record_block) + memcpy(rp, cbBlockPtr(m_recStartBlock+m_record_block)) — buffer block i = ring m_recStartBlock(record-time)+i. Playback reads pp_main=getBlock(m_play_block) = the BUFFER, not the ring. So m_recStartBlock-=floorBack at _startEndingRecording is INERT for audio (buffer already filled) but m_recordStartPhaseOffset-=floorBack DOES shift play_block=(masterPhase-offset)%L => playback lands offbeat by floorBack = the reported second-loop offbeat shift. DECISION: REMOVE the stop-time floor of recordStartPhaseOffset (and the inert m_recStartBlock floor). Keep recordStartPhaseOffset = the BEAT-SNAPPED record-start phase (set in _startRecording), which is exactly where buffer block 0's audio sits. Sync requires offset == buffer-block-0, which the record-start beat-snap already gives. The L-division-start the floor tried to add is NOT needed for sync (L in {M/8..} are all multiples of the M/16 beat grid, so the beat-snapped offset is coherent: play_block wraps at L and phase-zero == the recorded downbeat). The first loop is unaffected (wasFirst skipped). Witness: loopClipUpdate.cpp pp_main=getBlock; loopClip.cpp _startRecording memcpy fill; the floor block at _startEndingRecording is the shift source.
