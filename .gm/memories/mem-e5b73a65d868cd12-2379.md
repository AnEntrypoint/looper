---
key: mem-e5b73a65d868cd12-2379
ns: default
created: 1783799335917
updated: 1783799335917
---

{"text":"AIR192 continuous-crackle ROOT CAUSE + FIX (2026-07-11, commit 43b6aa9): The residual 'light continuous crackle' on the AIR192 440Hz round-trip is FM/pitch-jitter, NOT clicks/AM/quantization. Cross-validated WAV analysis (quadrature demod + independent zero-crossing, agreeing to 4 sig figs on C:\glitch.wav) found a harmonic comb at 26.382Hz (period 1819 samples / 37.9ms), RMS instantaneous-frequency deviation ~1.0-1.3Hz, amplitude clean (<0.25% AM), residual 47dB above 1 LSB on a fully-fine 16-bit grid (GCD of level spacings=1, so NOT quantization), and NOT aligned to the 64-sample/750Hz audio block boundary. CAUSE: the fractional-rate READ in patches/input_usb.cpp AudioInputUSB::update() (and the OTG tap patches/output_usb.cpp AudioOutputUSB_tapOTG) used 2-point LINEAR interpolation (m=m0+((m1-m0)*rd_frac)>>16). Under the AIR192's ~0.055% clock offset from the Pi's fixed 48kHz block clock (exactly 1 sample slip per 1819 samples), rate_step goes fractional and rd_frac sweeps 0..1 continuously; linear interpolation is a phase-dependent lossy lowpass whose amplitude/phase error, MODULATED by that wrapping fractional read phase, IS the 26.382Hz comb. FIX: replaced both readers with 4-point cubic Hermite (Catmull-Rom), Q16.16 fixed-point Horner in s64, guards tightened >1 to >2 for the rd+2 tap (rd-1 is always valid history). Witnessed via exec_js: matches float Catmull-Rom to 1.75 LSB max (~-85dBFS, inaudible) and cuts reconstruction RMS error 17.7dB at the exact measured 1/1819 drift. The rate-match mechanism is UNTOUCHED (the ring MUST be rate-matched or it under/overflows). KEY METHOD LESSON: three prior fixes (s_outBias smoothing, m_fbRate exponential smoothing, IN 32->s16 rounding) each measured ZERO effect because they all assumed the wrong defect (clicks/AM/quantization). The breakthrough came from MEASURING the actual signal's precise structure (AM vs FM, exact modulation freq, spectral comb) BEFORE proposing fix N+1. When N fixes measure zero effect, the defect is mischaracterized, not the code-candidate wrong. Deployed+live-confirmed: Pi BUID 'Jul 11 2026-19:24:41', tftproot kernel SHA256-identical to release build, inUR=0. FINAL on-hardware before/after comb witness still pending a fresh post-fix C:\glitch.wav capture (pre-fix capture was mtime 20:42, fix deployed 21:25).","key":"air192-crackle-fm-comb-cubic-interp-fix"}
