---
key: mem-558d1912c4aef16e-1120
ns: default
created: 1780134358391
updated: 1780134358391
---

## Resolved mutable: first-loop-tail-selfadvance-overlap

CONFIRMED via loopClipUpdate.cpp:28-33 (TAIL sets pp_main=getBlock(play_block) when record_block>0) + :174-202 (pp_main advance). First loop L>=masterLen takes else branch :198-202: m_play_block++ and at ==m_num_blocks calls _startCrossFade() -> CS_LOOPING + incDecRunning(+1) MID-TAIL. But _finishRecording (:148, gated CS_RECORDING_TAIL||CS_FINISHING at record_block>=max_blocks) then never fires because state is now CS_LOOPING; max_blocks=num_blocks+CROSSFADE so record_block hasn't reached it when play_block hit num_blocks. Result: TAIL never finishes cleanly, incDecRunning double-counts, loop enters LOOPING with crossfade_start=num_blocks set by _startCrossFade against a still-recording tail. This is the first-loop 'didn't repeat the exact region / funny place'. FIX DIRECTION: during TAIL, playback must NOT drive state transitions (no _startCrossFade); TAIL ends ONLY via _finishRecording at record_block>=max_blocks, which then _startPlaying at block 0. Playback advance must gate the crossfade/LOOPING transition on m_state==CS_PLAYING (not TAIL).
