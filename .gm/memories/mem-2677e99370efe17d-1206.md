---
key: mem-2677e99370efe17d-1206
ns: default
created: 1779718620243
updated: 1779718620243
---

## Resolved mutable: pi-output-pitch-51-not-55-plus-wobble

ROOT CAUSE on HDMI (rg-eng.png): lock=0 PERSISTENTLY (SNAC never locks on the Pi), gap swings 26868->9299->24755->7730, and emerg+1/+2 EVERY window — the emergency buffer-wrap escape is firing constantly. So: SNAC lock=0 => m_haveGoodPeriod likely false/stale => normal resplice never bounds the gap => gap runs to ~27000 => emergency escape fires (unaligned reset to m_wr-offset) => gap crashes to ~8000 => repeats. Those unaligned emergency resets ARE the detune+wobble (output ~51Hz wobbling). The engine LOCKS FINE ON HOST (lock 234/235 on identical clean input) but NEVER locks on the Pi. The incremental SNAC (snacBegin/detectPitchStep split across blocks, added in commit 8e6f99a) must be failing on the Pi specifically — the sweep never completes a valid detection. FIX: debug/repair the incremental SNAC so lock=1 on the Pi (then resplice bounds the gap, no emergency escapes, no detune/wobble). Suspect: the sweep state machine racing the SNAC ring writes, or m_snacPhase/m_snacK never completing, or the prefix-sum norm. Consider reverting to the single-shot detectPitch (now that Core1 has headroom) if incremental is the culprit.
