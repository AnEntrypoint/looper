---
key: mem-acec1f2041d1b36b-1653
ns: default
created: 1780828419849
updated: 1780828419849
---

## Looper SHIFT-hold routes the LOOPS INTO the effects (commit 36df5e7, supersedes 9da6324)

While APC25 SHIFT is held the running loops THEMSELVES become input to the live pitch+effects chain (loops get transposed/effected and are recordable through effects). SHIFT does NOT mute the loops - the earlier 9da6324 'gate loop output to silence, hear only live input' was WRONG (user: 'when we pressed shift the loops went silent instead of playing as the input into the effects'). Path: apcKey25::update publishes m_shift -> LiveParams.monitorMode -> loopMachine::update. update() reordered so loop output (updateState + per-track audio into m_output_buffer + Link block) is computed FIRST, BEFORE the pitch/effects stages (phase-neutral: clips read own buffer, m_masterPhase++ once/block). When held, the loop sum is folded into the effect source: m_input_buffer += m_output_buffer * m_loopFoldGain (ramps 0->1) BEFORE pLivePitchWrapper + pEffectsProcessor and before cbWriteBlock -> loops effected AND recorded wet. Dry loop suppressed complementarily: m_loopOutputGain = 1 - m_loopFoldGain, so loops heard once through effects, dry+fold==1 (no double-sum, no loudness jump). Live mic input still passes (loops added to it). Released fold->0/dry->1, loops return to dry output, phase-seamless. Both ramps 1/16 per block, per-sample interpolated, click-free. :4445 TIME loopGate = dry gain*100 (100=dry, 0=fully into effects). Witnessed scripts/test-monitor-route.cpp ALL PASS (complementary/energy-conserved, click-free, endpoints, rapid-toggle bounded, phase-neutral); test-monitor-gate.cpp deleted; clean build kernel7l.img 1061196B; flashed to SD E:.
