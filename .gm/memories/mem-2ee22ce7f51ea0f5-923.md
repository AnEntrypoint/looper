---
key: mem-2ee22ce7f51ea0f5-923
ns: default
created: 1780212544904
updated: 1780212544904
---

## Resolved mutable: phrase-not-120-normalized

abletonLink.cpp:233-272: linkDeriveQuant computes nearest-120 beats+bpm from clip_seconds; linkEnd stores s_bpm/s_quantBeats but loopClip.cpp:198 sets m_masterLoopBlocks=m_num_blocks (RAW first-loop length). DECISION (505-like, req1): keep the RAW first-loop length as the phrase M. Reason: the first loop IS the musical reference the operator played; re-snapping M to a 120-normalized block count would shift the first loop's own playback off its recorded region (the exact bug we just fixed - first loop must loop its exact region). The 120-nearest derivation correctly sets the BROADCAST tempo (s_bpm for Link peers) WITHOUT altering local M. Consecutive loops align to the raw M. So req1 'right length and speed' = the derived bpm is the 120-nearest, the phrase blocks stay the operator's first loop. No change to m_masterLoopBlocks; the consecutive-grid bug is NOT here.
