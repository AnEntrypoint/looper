---
key: mem-b8260f47b7ea3dfe-1867
ns: default
created: 1782319423927
updated: 1782319423927
---

## Keybed channel-isolation -- complete audit (commits d5d2a43 + fec6b29, 2026-06-24)

INVARIANT: APC keybed = MIDI channel 1 (sampler + transpose/live-pitch ONLY). Buttons/pads/transport/SHIFT/glitch = channel 0. ch2 = UDP-MIDI test inject. Effects/filters = CC (the 8 knobs), channel-agnostic -- a KEY PRESS is a NOTE not a CC, so a key can NEVER trigger an effect/filter; glitch (microrepeat 82-86) is NOTE-triggered so it MUST be channel-gated.

TWO LAYERS, both now isolated:
1. ACTIVE input path = hardcoded apcKey25.cpp handleMidi. ch1 note block (103-126) returns on every path (sampler 115/119, transpose 125) -> never reaches microrepeat/pad/_onButton (all ch0-gated). SHIFT(91/167)+microrepeat(140/177)+pad(158/216)+_onButton(163) gated to channel==0. (midiMapResolveInput is NOT used for input -- only midiMapResolveOutput drives LEDs.)
2. DATA layer = midiMap.h g_apc25Inputs (stated future input path). Was MIDI_ANY_CHANNEL for MA_SHIFT/MA_MICROREPEAT(82-86)/MA_PAD/transport -> latent overlap. Now channel 0. Keybed rows ch1(263)/ch2(264) + transpose CCs (modwheel CC1, CC52) + filter/effect CCs unchanged (knobs).

WHY note 98 mattered: APC25_BTN_SHIFT=0x62=98=D; keybed +3 octaves sends 98 -> pre-fix the unconditional SHIFT check swallowed it ('3 octaves up D doesnt play').

PARITY: scripts/test-midi-config-parity.cpp legacyInputTag reference updated to the same ch0 gating; the test asserts profile-resolution == reference across all 16 channels -> profile + reference + real handler agree. ALL PASS (+ sampler/microrepeat/monitor-route). Kernel 1095324B deployed to tftproot. midiMap.h profile change is LED-path only at runtime (input is hardcoded) so binary behavior unchanged; value is consistency + the parity guard + future-proofing. See [[looper-keybed-channel-and-sampler-fx-fixes]] [[looper-midimap-profile]] [[looper-microrepeat-full]].
