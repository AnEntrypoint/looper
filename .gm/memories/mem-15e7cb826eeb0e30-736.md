---
key: mem-15e7cb826eeb0e30-736
ns: default
created: 1779831086047
updated: 1779831086047
---

## Resolved mutable: net-stack-crash-at-90s

SUPERSEDED + addressed in code. Root cause was NOT a generic ~90s net-task assert — it was the cross-core pollSockets(Core0)/m_Net.Process(Core2) race surfacing as netbuffer.cpp(102) ~CNetBuffer assert(!m_pNext). Fixed by pinning all net access to Core 2 (commit 02a7133, kernel 1068676B). Awaiting on-hardware netboot verification — externally blocked on a physical Pi power-cycle that actually re-fetches via TFTP (4 cycle prompts so far left HDMI byte-frozen md5 93e71087 on the prior v3 crash with no new fetch; live TFTP server pid322656 verified serving correct file md5 99193bc8 from disk via a localhost RRQ test this turn, so the box will get the fix the moment it re-requests).
