---
key: mem-6015619f828459b5-1041
ns: default
created: 1779712813935
updated: 1779712813935
---

## Resolved mutable: gap-monotonic-drift-clock

REFINED via gap+emerg telemetry during gurgle (.gm/hdmi-cap/rf-gurgle.png): emerg+0 throughout (emergency escape NOT involved). Gap swings 20962 -> 2935 -> 18198 -> 525. The cycle is: lock=0 persists for seconds while gap RUNS UP to ~20000 with splice+1 (resplice NOT firing during lock=0!), then lock=1 returns and splice STORMS +180 to pull the 20000 gap back = gurgle, gap drops to ~525, repeat ~16s. TWO bugs: (1) during lock=0 the resplice barely fires (splice+1) despite m_haveGoodPeriod stale period being available + gap huge — so something gates it off (suspect m_envSlow, or m_haveGoodPeriod actually false, or the splice cooldown mis-set); gap runs unbounded. (2) when lock returns, the single clamped multi-period jump can't clear a 20000 gap in one go (clamped to <= wr-offset) so it storms. FIX: (a) make resplice ACTUALLY fire during lock=0 to bound the gap (debug why splice+1), and (b) on lock-return with huge gap, do ONE clean reset-to-target (phase-coherent) not a storm.
