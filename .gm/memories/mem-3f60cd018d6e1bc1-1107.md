---
key: mem-3f60cd018d6e1bc1-1107
ns: default
created: 1782296789630
updated: 1782296789630
---

## Resolved mutable: latency-source-uac2-out

PINNED. usbaudiodevice.cpp:190-198 CUSBAudioDevice::Configure submits BOTH OUT URBs (StartOutRequest(0); StartOutRequest(1)) UNCONDITIONALLY for every device incl UCA222 -> double-buffered (2 OUT URBs in flight). Pre-Tascam (ab3dd58^) submitted ONE OUT URB. For full-speed UCA222 each URB=1ms, so double-buffer = +~1ms OUT latency. The commit comment (a138057) says double-buffering was for 'the residual Tascam under-feed' and the full-speed UCA222 'never showed this' -> NOT needed for UCA222. The avail=256 PI rate-trim + N=8 multi-iso are already UAC2-gated (inside if(m_bUAC2 && m_pEndpointFb), usbaudiodevice.cpp:353) so they DON'T affect UCA222. Secondary minor add: output_usb.cpp:165 reader seeds wr_snap-128 vs pre-Tascab resync wr-nSamples*2 (~96) = +~32 samp ~0.67ms. FIX: gate the double-buffer on m_bUAC2 (UAC1/UCA222 submits only StartOutRequest(0); OutCompletion already re-arms per-slot so single-buffer just works); optionally restore the UAC1 reader seed to ~nSamples*2. Net: UCA222 back to single-URB low latency, Tascam keeps double-buffer.
