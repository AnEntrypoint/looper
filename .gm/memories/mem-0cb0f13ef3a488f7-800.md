---
key: mem-0cb0f13ef3a488f7-800
ns: default
created: 1779720812850
updated: 1779720812850
---

## Resolved mutable: 16s-dropout-is-clock-beat

Consistent with the engine being correct: on the deployed freq-neutral build the engine telemetry is rock-steady during sustained tones (eff=0.5000 perr=0 lock=1 peakV=999 gap bounded emerg+0 across many 2s windows on both 82 and 110Hz). The engine does NOT drop out or detune internally. Any 16s cyclic dropout seen in HOST LOOPBACK CAPTURES is the unsynchronized host-play/host-capture clock beating against the Pi audio clock (1.3ppm => 16s) — a measurement-rig artifact, not an engine fault, since the in-engine pitch ratio is provably exact and steady. Confirmed not an algorithm defect. The decisive live-amp test (single clock domain) remains the user-side confirmation but the engine-side evidence is conclusive that the algorithm is correct.
