---
key: mem-cdd137d397ab4919-1129
ns: default
created: 1779712824383
updated: 1779712824383
---

## Resolved mutable: drain-hunting-needs-hysteresis

Latched hysteresis (b588008) did NOT fix it (35/45 gurgle, 40-50 clicks/s steady). REFRAME: DRY passthrough through the SAME ring is perfectly clean (0.07% THD) — so the IN ring is NOT the problem. The ~40-55 clicks/s during -12 ≈ the engine's intrinsic splice rate (55/s): at scale 0.5 the reader falls 1 period behind every 2 periods, so single-reader resampling PSOLA MUST resplice ~55/s to bound the gap — that rate is architectural, not tunable. On HOST (perfect synthetic sine) consecutive periods are identical so the 1-period-jump crossfade is seamless (THD 0.4%). On the Pi the real input (rig/ADC noise) makes consecutive periods slightly DIFFERENT, so each blind 1-period jump in triggerSpliceByPeriod has a small discontinuity = 55 clicks/s = the gurgle/buzz. FIX: triggerSpliceByPeriod blindly jumps exactly per; it must REFINE the splice point (value+derivative match within +/- per/2, like the old triggerSplice m_spliceMatch path) so each crossfade lands on a waveform-matched point = seamless even on imperfect input. The drain tuning was a red herring.
