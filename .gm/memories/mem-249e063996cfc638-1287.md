---
key: mem-249e063996cfc638-1287
ns: default
created: 1779714159804
updated: 1779714159804
---

## Resolved mutable: beat-reproduced-on-host

PIVOTAL CORRECTION: there is NO dropout. Wideband (50-60Hz) energy on the Pi capture (fi45) is STRONG and STEADY (0.71-1.05) across ALL 45 seconds, including the 'bad' seconds where my narrow Goertzel at exactly 55.0Hz collapsed to 0.02. The -12 output is always full-amplitude — it WARBLES in frequency around 55Hz (±~3% = ±50 cents) on the ~16s cycle. My fixed-55.0Hz THD metric mismeasured the warble as a dropout. Host beatdiag confirms: during the beat, lock=1, splice ~7/s steady, but the detected PERIOD oscillates 425<->450 (±3%) tracking the input rate. So the engine faithfully follows the IN-ring's drift-correction rate oscillation (±3%), and that shows as output PITCH WARBLE = the audible gurgle. The host WAV renders have a steady clock so no warble. FIX: the warble = the ring delivering input at an oscillating rate; the engine reads the delay-line at fixed m_scale so input-rate variation passes through as pitch warble. Either (a) the ring drift-correction oscillation is too deep (±3% — should be <0.5%) but that's the forbidden ring, or (b) the engine LOW-PASSES its effective pitch (smooth m_scale/read-rate over seconds) so fast input-rate wobble is rejected while the true -12 ratio is preserved. Pursue (b).
