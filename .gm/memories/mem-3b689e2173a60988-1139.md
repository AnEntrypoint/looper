---
key: mem-3b689e2173a60988-1139
ns: default
created: 1779743281780
updated: 1779743281780
---

## Resolved mutable: anchor-discards-bestoff-clicks

PARTIALLY ADDRESSED but NOT the click source. Deployed the integer-period-match splice (build 1047764B, commit 20a9989): pitch stays exact (eff=0.5000 perr=0) and the user reports the sound is now OTHERWISE VERY SMOOTH (the splice character improved — no roughness/gurgle), BUT the same clicking persists. So the per-splice crossfade was NOT the click source. Reframe: telemetry shows splice+6/2s=3/sec but in_rs+18/2s=9/sec. The clicking rate is more consistent with the IN-RING RESYNC (in_rs ~9/sec, arg dipping to 49=underrun edge) than the splice rate. A ring resync jumps the read position => a discontinuity in the data WRITTEN into the engine delay line => the engine reads straight through it => click. Pitch is unaffected (exact) but each resync = one click. Next: absorb the ring-resync discontinuity IN THE EFFECT (honor do-not-touch-ring constraint) — e.g. the engine detects a write-position jump and crossfades across it, OR reduce what drives the resync. Investigate patches/input_usb.cpp resync trigger + whether the engine can smooth a detected input discontinuity.
