---
key: mem-1948fb2abd64fe8f-1238
ns: default
created: 1780234861383
updated: 1780234861383
---

## Resolved mutable: latched-recstart-reads-unwritten

CONFIRMED via ordering: loopMachine::update does (609) cbWriteBlock [writes head W, g_cbWriteBlock->W+1] then (655) updateState() [latch -> _startRecording captures m_recStartBlock=g_cbWriteBlock=W+1] then (664) pTrack->update [new clip records record_block=0 reading cbBlockPtr(m_recStartBlock+0)=cbBlockPtr(W+1)]. W+1 is the NEXT-to-write block (cbWriteBlock writes dst THEN ++), so it holds stale ~180s-old ring content / silence, NOT current audio. So the consecutive clip records SILENCE at its head => plays nothing ('records then no audio'). The first loop uses cbBackdatedBlock=W+1-backBlocks (backBlocks>=1) = written history, so it reads real audio. FIX: latched consecutive anchor must point at WRITTEN audio. Use m_recStartBlock = cbBackdatedBlock(0) (= g_cbWriteBlock - CB_FIXED_LAG_SAMPLES/AUDIO_BLOCK_SAMPLES, lands on written history AND applies the same fixed ring/ADC lag the first loop uses for consistency), keeping recordStartPhaseOffset = m_masterPhase (the latch beat). cbBackdatedBlock(0) uses fixed-lag-only (press_ticks==0), no stale g_pendingPressTicks. Witness continuousBuffer.cpp cbWriteBlock write-then-increment + loopMachine.cpp:609/655/664 ordering.
