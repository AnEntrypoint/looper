---
key: mem-f80281c1768b5be6-772
ns: default
created: 1779816947771
updated: 1779816947771
---

BUILD DISCIPLINE (load-bearing): firmware make runs CHECK_DEPS=0 = NO header-dependency tracking. Editing a .h (soladSnacOctaver.h/grainFormant.h/RubberBandWrapper.h) does NOT recompile the .o that include it (loopMachine.o/audio.o); make only rebuilds .o with a newer .cpp. So after ANY header edit you MUST rm the dependent .o (or all *.o) before make, else you deploy STALE engine code while source looks right. This caused a whole session of host-works-Pi-doesnt. Symptom: live telemetry showed fdepth updating (cpp-reached) but gTgt/gMix not (same header inline setFormantDepth) = stale .o. After rm *.o, regenerate wlan_firmware.o via arm-none-eabi-as from $TEMP/wlan_firmware.S (rm deletes it, no make rule). Clean rebuild size 1055052->1055820 confirmed recompile.
