---
key: mem-90716c53618677f1-1019
ns: default
created: 1779832782919
updated: 1779832782919
---

## Resolved mutable: diag-fmf-stuck

RESOLVED to a precise external gate. On the stable Core-2-fix kernel (1068676B, survived 230s), live :4445 queries prove the knob->param wiring is CORRECT: fdepth tracks CC53 (0->-1.0159, 127->1.0, 64->0.0) and gTgt tracks (0.5/2.0/1.0). But gFac/gMix/scale/eff stay frozen at init (1.0/1.0/0.0) and lock=0 period=256 across 230s+ because the engine processBlock only runs when AUDIO BLOCKS flow into the Pi (loopMachine.cpp:557 gates feedAudio/retrieveAudio on lp.liveEngaged AND the AudioSystem update, which only fires with live USB-audio input). On a silent diagnosis boot (nothing plugged into UCA222 IN) the engine never processes, so gMix cannot ramp regardless of correctness. The original diag-fmf-stuck (CHECK_DEPS stale .o) was already fixed; the formant gMix ramp is now verifiable ONLY with live audio into the Pi. blockedBy external: needs guitar/tone into UCA222 IN. PRD engage-formant-cannot-be-exercised-without-live-audio captures the full code trace + next steps.
