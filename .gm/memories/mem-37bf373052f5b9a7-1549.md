---
key: mem-37bf373052f5b9a7-1549
ns: default
created: 1783807058547
updated: 1783807058547
---

AIR192 2048-tick UPDATE (2026-07-11): user confirmed the ~6 ticks/sec (one-sample dropout, exact 2048-sample/23.44Hz grid) occur with PITCH SHIFT OFF (plain passthrough). This DEFINITIVELY eliminates SNAC/octaver (loopMachine.cpp:759 gates feedAudio/retrieveAudio behind if(liveEngaged) -- with pitch off they never run; SNAC_HOP=2048 was a coincidental match). Also eliminated by static read: loopMachine::update passthrough output (loopMachine.cpp:950-968: output = ival(dry input)+oval(loops, =0 when idle), sample-by-sample, NO ring or 2048 boundary; cbWriteBlock at :901 is a write-only continuous-buffer tap that never feeds back into the output). Full elimination list now: NOT SNAC, NOT loopMachine passthrough, NOT resync/underrun/OUT-clamp (live), NOT reverb/delay, IN read unity (inRateStep=65536). REMAINING: a USB/DMA-layer 2048 boundary (USB_AUDIO_INBUF_BYTES=2048 is the only 2048 in the USB path but it's bytes=256 frames, URB carries only ~48 frames), OR device-side (AIR192 internal converter block). DECISIVE TEST (reachable once the tone plays, NO new build needed): capture MLONG (8192 mono IN samples = 4x2048) during a live 440Hz tone -- the Pi IN is the round-tripped analog loopback; if it shows the 2048-tick, the artifact is in the analog round-trip; cross-check with a Pi digital-OUT capture to isolate Pi-OUT vs device. BLOCKER: the AIR192 is silent when idle; needs the user playing the tone during the capture. Diagnostic already shipped: eng= field on :4445 TIME (commit d13dba8) to confirm pitch-off during the tick.
