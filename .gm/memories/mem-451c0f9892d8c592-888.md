---
key: mem-451c0f9892d8c592-888
ns: default
created: 1782307528465
updated: 1782307528465
---

## Resolved mutable: sampler-3oct-d-silent

sampler.h renderInto: voice attack ramps gain 0->1 at GAIN_STEP=1/48 (needs >=48 samples). chromatic rate=pow(2,(note-60)/12); 3 octaves up (note ~96-99) -> rate ~8-9.5x, so a chromatic sample of length L finishes in L/rate samples and the end-check (pos>=len-1 -> target=0, release) fires after L/rate samples. If L/rate < ~48 (attack not yet complete), the voice releases before gain reaches audible -> SILENT. The specific 'D' 3-oct up is the note where L/rate crosses below the attack window for the loaded sample. Also _readInterp returns 0 once i0>=len. FIX options: scale the attack to the voice's playable length (fast attack for short/fast voices), and/or clamp the max playback rate so very high notes still render >= a few attack samples. Witness: sampler.h renderInto GAIN_STEP attack vs pos+=rate end-release; _noteOn rate formula.
