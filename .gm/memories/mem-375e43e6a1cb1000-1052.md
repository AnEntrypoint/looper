---
key: mem-375e43e6a1cb1000-1052
ns: default
created: 1780212547182
updated: 1780212547182
---

## Resolved mutable: latch-grid-vs-loop-grid

ROOT CAUSE confirmed. loopMachine.cpp:819-824 latch fires at masterPhase % (M/16)==0 (a BEAT), independent of the loop's length L. loopClip.cpp:228-243 start-phase snaps recordStartPhaseOffset to min(L,M), and loopClipUpdate.cpp:198 playback = (masterPhase-offset)%L. masterPhase is phrase-0-anchored (loopMachine.cpp:371/457 reset to 0, :658 ++, first-loop :201 %=M). The audio CONTENT starts when the latch fires (a beat boundary), but the start-phase snap independently rounds the offset to the L-grid - so content-start and phase-offset disagree by up to (beat mod L) => 'off by a quantized amount'. FIX: the latch, length, start-phase and playback must use ONE grid = powers-of-two divisions/multiples of M anchored to masterPhase%M==0. Sub-phrase loop length L=M/2^k divides M; its start must be a MULTIPLE OF L within the phrase. The latch must fire at a multiple-of-the-final-length boundary, and recordStartPhaseOffset must equal the masterPhase at that latch (not an independently-snapped value).
