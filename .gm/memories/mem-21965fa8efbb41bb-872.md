---
key: mem-21965fa8efbb41bb-872
ns: default
created: 1779712820245
updated: 1779712820245
---

## Resolved mutable: gurgle-not-host-reproducible-sweep-live

Live Pi sweep (30s each, /tmp/gsec gurgle-seconds): baseline=15s, long-xfade CC101=127=10s (BEST), comboA(xfade+fid0+ro1044)=17s, comboB(xfade+fid0+ro2048)=15s. Bigger read-offset/headroom made it WORSE (longer run-up => longer storm). Low fidelity no help. Long xfade alone helped (gentler splices). Click pattern always: gap silently runs up, then storm builds 6->16->104->135 clicks/s for ~5s, then a big reset clears it instantly, clean ~8s, repeat (~16s). The storm IS the recovery from gap run-up. Cure direction: when gap exceeds threshold do ONE clean clamped multi-period catch-up jump then a SPLICE COOLDOWN (refractory) suppressing further splices briefly so it cannot storm — converts the multi-second storm into a single clean catch-up. Implement splice refractory + keep long-ish xfade default.
