---
key: mem-cba46114be04aa8e-1036
ns: default
created: 1782320916416
updated: 1782320916416
---

## Resolved mutable: long-uptime-latency-creep-cause

CONFIRMED + already fixed in the deployed kernel. CAUSE = USB OUT-ring lag drift on the UCA222/UAC1 path: pre-b4d4f6c outHandler read 1:1 with overflow-only resync, no rate correction -> the engine(44100->48000)-vs-DAC clock mismatch made the OUT 'avail' (=latency) climb monotonically toward overflow over uptime (slow sawtooth) = growing latency. Witness: git show b4d4f6c diff (my own comment: 'UAC1 ... has NO send-rate PI, so without help here the read position drifts and the OUT lag creeps up'). FIX b4d4f6c = deadband skip/repeat trim toward OUT_RD_TARGET=96, pins lag ~2ms, cannot creep -- present in patches/output_usb.cpp:170-191 and in the deployed kernel (dist==tftproot md5 21d9c0bc..., built this session post-fec6b29). The user's PRIOR flash predates b4d4f6c. RULED OUT: IN ring already drift-corrected (input_usb.cpp IN_TARGET_LAG+IN_DEADBAND+fractional), m_ageCtr wrap cosmetic/~497d, m_delayReadPos modulo-bounded, s_outBias clamped+UAC2-only. No new code needed.
