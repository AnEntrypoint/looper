---
key: mem-233fa1e4d5e0918d-1289
ns: default
created: 1780233922369
updated: 1780233922369
---

## Resolved mutable: second-loop-no-play-cause

CONFIRMED candidate A via scripts/test-second-loop-play.cpp + user: symptom 'records then silent/never starts'. _calcQuantizeTarget(M>0) picks the nearest {M/8..8M} candidate to m_record_block BY DISTANCE — for a short consecutive tap (recorded=200) nearest is M/8=690 > recorded, so updateState(PLAY) takes the ELSE branch: m_quantizeTarget=690, m_quantizeWillPlay=true (DEFERRED), clip stays CS_RECORDING_MAIN waiting for m_record_block>=690. If the operator stopped playing into it / it never reaches target, the clip never finishes -> never plays (records then silent, never starts). User decision: round DOWN for short (sub-phrase) so target<=recorded and it PLAYS NOW; round UP only when near/over a full phrase (extend to the phrase multiple, 505 play-through). FIX _calcQuantizeTarget: for recorded < M (sub-phrase region) choose the largest division {M/8,M/4,M/2} that is <= recorded (floored to CROSSFADE*2) so it plays immediately; for recorded >= ~M choose the nearest phrase multiple {M,2M,4M,8M} (round to nearest, may defer to extend). This makes every consecutive stop yield a playable clip. NOT the latched-anchor change itself (that fixed the late-start); this is the quant-direction that left short loops stuck deferred.
