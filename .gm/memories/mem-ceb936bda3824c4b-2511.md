---
key: mem-ceb936bda3824c4b-2511
ns: default
created: 1783545519289
updated: 1783545519289
---

## Resolved mutable: multipacket-urb-resultlength-semantics

Read C:\Users\user\.looper-build\circle\lib\usb\dwhcixferstagedata.cpp:194-297 (TransactionComplete) and :578-586 (GetResultLen). CONFIRMED BUG: GetResultLen() returns m_nTotalBytesTransfered clamped to m_nTransferSize (line 580-583: 'if (m_nTotalBytesTransfered > m_nTransferSize) return m_nTransferSize;'). m_nTotalBytesTransfered correctly ACCUMULATES across all N AddIsoPacket'd packets (line 248, incremented every TransactionComplete call). But m_nTransferSize is REASSIGNED each packet iteration to ONLY the current packet's size (line 279: 'm_nTransferSize = m_pURB->GetIsoPacketSize(m_nIsoPackets)') -- it is never the running sum. By the time the last (8th) packet completes and the stage is IsStageComplete()==true (m_nPackets==0), m_nTransferSize holds packet 8's size alone (a few dozen bytes), while m_nTotalBytesTransfered holds the SUM of all 8 packets (several hundred bytes) -- triggering the clamp and returning only ~1/8th of the actual received data as GetResultLength(). This vendored Circle DWHCI driver was written/tested against single-packet iso URBs (OUT's pre-existing pattern in StartOutRequest also uses AddIsoPacket N=8 times but NEVER reads GetResultLength() on completion -- it's a send, doesn't care). My IN-side StartInRequest is the FIRST code in this codebase to both submit a multi-packet iso URB AND read GetResultLength() on completion, exposing a latent bug in the vendored USB stack's multi-packet result-length clamp. CONSEQUENCE: InCompletion's nSamples=GetResultLength()/frameBytes is silently truncated to roughly 1/8 of the real sample count every completion, but the buffer m_InBuf DOES contain all 8 packets' worth of valid data (m_pBufferPointer correctly advances by nBytesTransfered each packet, line 249) -- so InCompletion reads a WRONG (too-small) sample count from the FRONT of a buffer that's mostly valid data, silently dropping ~7/8 of every completion's samples. This does not corrupt the samples it DOES read (they're real front-of-buffer data) but it massively under-delivers new audio to the ring per completion -- explains continuous distortion/buzz: the IN ring starves relative to real-time, forcing constant resync/repeat-sample fallback in AudioInputUSB::update (input_usb.cpp IN_RING resync-on-empty and rate-trim), which is exactly the buzzy/distorted signature (not clean total silence, not intermittent glitch -- continuous corrupted audio from constant ring starvation+resync).
