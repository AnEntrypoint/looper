---
key: mem-d6d2add3e01e7730-871
ns: default
created: 1779712822108
updated: 1779712822108
---

## Resolved mutable: pi-wedged-needs-power-cycle

CONFIRMED USB-controller wedge needing full power-cycle (soft REBOOT insufficient). HDMI .gm/hdmi-cap/hy-boot.png: firmware boots (ts 00:00:25, cores c1=12%) but USB enumeration FAILS — 'xhciep: Transfer error 4 on endpoint 1', red 'usbhub: Cannot get port status (port 1)', 'Function is not supported', and in_rs+1(arg=96096..96096 = garbage). The UCA222 audio device on USB port 1 never enumerates, so no audio loopback + no clean net. The soft REBOOT (UDP:4444) restarts the kernel but does NOT reset the DWC2/xHCI USB PHY, so the wedged USB state from the many rapid netboot cycles persists across soft reboots. The latched-hysteresis fix (b588008) is built + staged in tftproot. Needs a FULL POWER-CYCLE (pull power, wait, replug) to reset USB; it will then netboot b588008 cleanly and the audio path will come up.
