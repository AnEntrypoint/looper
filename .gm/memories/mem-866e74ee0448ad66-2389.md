---
key: mem-866e74ee0448ad66-2389
ns: default
created: 1781121333260
updated: 1781121333260
---

## Looper sampler subsystem (buttons 65/66) + filters moved to chain end (commit f6882d5)

patches/sampler.h (pSampler, alloc audio.cpp like pMicroRepeat) = independent sound SOURCE, NOT a looper (touches no clip/masterPhase). Mixed into m_input_buffer at the TOP of loopMachine::update (before loop-fold/pitch/effects/microrepeat/filters) so samples get all effects and are recordable into a loop (SHIFT folds them into a recording loop). Capture reads the DRY mic snapshot BEFORE renderInto (captureBlock before renderInto) so a sample never records itself/loops. Button 65 (0x41, ch0) HELD records ONE chromatic sample; on release auto-trims silence; 25 keys play it chromatically rate=2^((note-60)/12), middle C=60=orig, poly. Button 66 (0x42, ch0) HELD = drum mode: holding a key records into that key's drum slot (keyIndex=note-48, 25 slots, BASE_NOTE=48), auto-trim on release; loaded drum plays original pitch one-shot, OVERRIDES chromatic on that key. Keyboard routing (apcKey25 handleMidi ch==1): drumRecordMode->EV_REC_START(keyIdx); else chromaticLoaded||drumLoaded(key)->EV_NOTE_ON/OFF (live-pitch keyboard transpose SUPPRESSED; live pitch still via CC1 mod-wheel/CC52); else fall through to live pitch. ISR->Core1 via lock-free SPSC EV ring (pushEvent/_drainEvents reads fields individually since volatile struct copy fails); s16 heap buffers (5s chrom + 25x2s drum ~10.5MB); 16 voices oldest-steal; click-free GAIN_STEP attack/release + EDGE_FADE trim; overrun clamps (captureBlock stops at maxLen, _stopRecord finalizes on pending target m_recTarget!=-2). midiMap MA_SAMPLER_RECORD/DRUM_MODE/SAMPLER_KEY (ch1 runtime overlay on MA_LIVE_PITCH_NOTE_CH1). Filters moved to chain END (after microrepeat glitch): apcEffectsProcessor split processFilters(HP/LP)+processSends(delay/reverb); loopMachine runs sends, microrepeat, then processFilters before cbWriteBlock; byte-exact passthrough at default knobs (HP0/LP1). :4445 TIME: sampRec/drumMode/sampLen/drumLoaded/voices. Witnessed scripts/test-sampler.cpp + test-filter-order.cpp ALL PASS; regression suite ALL PASS; clean build kernel7l.img 1065932B (full rebuild after apcKey25.h layout change); flashed SD E:. BUILD GOTCHA: build-local incremental does NOT rebuild siblings of a changed header under CHECK_DEPS=0 (apcKey25Notes/Transpose/Filters.cpp include apcKey25.h) -> rm *.o + regen wlan_firmware.o for clean rebuild.
