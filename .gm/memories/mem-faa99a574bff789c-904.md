---
key: mem-faa99a574bff789c-904
ns: default
created: 1782307639247
updated: 1782307639247
---

## Resolved mutable: keybed-channel-overlap

CONFIRMED + unifies BUG3. apcKey25.h:15 APC_BTN_SHIFT=0x62=98. 98%12=2=D. The APC keybed at +3 octaves (BASE 48-72 +36 = 84-108) sends note 98 for that D. apcKey25.cpp:91 (note-on) `if (data1 == APC_BTN_SHIFT) { m_shift=true; return; }` and :162 (note-off) run BEFORE the `if (channel==1)` keybed block with NO channel guard -> the keybed D(98) is swallowed as SHIFT and never reaches the sampler -> 'D doesnt play at 3 octaves up'. Also note-on line 122 `if (channel==2)` precedes ch1 (ok, different channel). The microrepeat(134)/pad(151)/_onButton(156) are AFTER the ch1 return so keybed doesn't reach them, but SHIFT(91/162) does. FIX: gate the SHIFT check (and confirm every pre-ch1 handler) to channel==0 so the keybed channel(1) is exclusively sampler/live-pitch and no button function intercepts it. Resolves both the channel-overlap request AND bug3.
