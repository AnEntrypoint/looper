[
  {
    "id": "split-apckey25-file",
    "subject": "Split apcKey25.cpp into core + transpose handlers",
    "status": "pending",
    "description": "apcKey25.cpp exceeds 200-line limit (294 lines). Split into apcKey25.cpp (core: ctor, handleMidi main logic, update) and apcKey25Transpose.cpp (transpose handlers). Update Makefile OBJS.",
    "effort": "small",
    "category": "refactor",
    "blocking": [
      "handle-ch2-note-transpose"
    ],
    "blockedBy": [],
    "acceptance": [
      "Both files under 200 lines",
      "No compilation errors",
      "Functionality preserved"
    ],
    "edge_cases": [
      "Include guards correct",
      "extern declarations as needed"
    ]
  },
  {
    "id": "handle-ch2-note-transpose",
    "subject": "Map channel 2 notes to transpose semitones (0-11)",
    "status": "deferred",
    "description": "[DEFERRED] When m_transposeLocked=true and ch2 (0x91/0x81) Note On received, map note to semitone: (note % 12). Store in m_transposePitch. Only active when transposeLocked.",
    "effort": "small",
    "category": "feature",
    "blocking": [
      "convert-semitones-to-ratio"
    ],
    "blockedBy": [
      "split-apckey25-file"
    ],
    "acceptance": [
      "Ch2 notes map to 0-11 semitones",
      "m_transposePitch updated only when locked",
      "No effect on ch1 pad notes"
    ],
    "edge_cases": [
      "Note received when not locked (ignored)",
      "Overlapping notes on ch2 (latest wins)"
    ]
  },
  {
    "id": "handle-cc52-fine-offset",
    "subject": "Map CC 52 (channel 1) to -6 to +6 fine offset",
    "status": "pending",
    "description": "CC 0xB0 (Control Change on ch0), CC 52: map data2 (0-127) to m_pitchWheelOffset range -6 to +6. Also update m_driftTarget=0 when CC moved (arm drift logic).",
    "effort": "small",
    "category": "feature",
    "blocking": [
      "convert-semitones-to-ratio"
    ],
    "blockedBy": [
      "add-transpose-lock-state"
    ],
    "acceptance": [
      "CC 52 value 0-127 maps to offset -6 to +6",
      "Offset updates atomically",
      "Drift target reset when CC moves"
    ],
    "edge_cases": [
      "CC 52 received when not locked (still updates offset)",
      "Pitch wheel (CC 1) vs CC 52 coexistence (both add to semitones)"
    ]
  },
  {
    "id": "implement-pitch-drift",
    "subject": "Auto-drift pitch wheel offset toward 0 when released",
    "status": "pending",
    "description": "In apcKey25::update(), if m_pitchWheelOffset != m_driftTarget (0.0): drift at rate -sign(offset)*driftPerMs until within 0.5. driftPerMs = 0.01 (offset changes -1 per 100ms when >0.5 away). Track m_lastDriftMs.",
    "effort": "small",
    "category": "feature",
    "blocking": [],
    "blockedBy": [
      "add-transpose-lock-state"
    ],
    "acceptance": [
      "Offset drifts toward 0 when idle",
      "Drift rate smooth and perceptible",
      "Stops when within ±0.5"
    ],
    "edge_cases": [
      "User moves CC during drift (resets target to 0)",
      "Very rapid CC moves (no overshoot on drift)"
    ]
  },
  {
    "id": "convert-semitones-to-ratio",
    "subject": "Convert transpose semitones + offset to tempo ratio",
    "status": "pending",
    "description": "Function in apcKey25 or audio.cpp: given (m_transposePitch + m_pitchWheelOffset) in semitones, return tempo ratio = 2^(semitones/12). Call this when transpose state changes or offset updates. Store result for wrapper use.",
    "effort": "small",
    "category": "refactor",
    "blocking": [
      "wire-transpose-to-wrapper"
    ],
    "blockedBy": [
      "handle-ch2-note-transpose",
      "handle-cc52-fine-offset"
    ],
    "acceptance": [
      "Semitone to ratio conversion correct (e.g., 12 semitones = 2.0x)",
      "Fractional semitones handled (e.g., 6.5 = sqrt(2))",
      "No NaN/Inf on edge cases"
    ],
    "edge_cases": [
      "semitones = 0 (ratio = 1.0)",
      "semitones = -12 (ratio = 0.5)",
      "semitones = 24 (ratio = 4.0)"
    ]
  },
  {
    "id": "wire-transpose-to-wrapper",
    "subject": "Gate live pitch wrapper: only run when transposeLocked",
    "status": "pending",
    "description": "In audio.cpp loop(), only call pLivePitchWrapper->updateRatios() if m_transposeLocked. When unlocked, wrapper idle (no processing). When locked, setTempoRatio() applies current transpose+offset.",
    "effort": "small",
    "category": "feature",
    "blocking": [],
    "blockedBy": [
      "handle-ch1-note64",
      "convert-semitones-to-ratio"
    ],
    "acceptance": [
      "Wrapper inactive when not locked (CPU saved)",
      "Wrapper active and applies transpose when locked",
      "No audio glitch on lock/unlock transition"
    ],
    "edge_cases": [
      "Lock while audio playing (smooth transition)",
      "Unlock mid-playback (no pop)",
      "Rapid lock/unlock (no CPU spike)"
    ]
  },
  {
    "id": "observability-transpose-state",
    "subject": "Add debug output for transpose lock state",
    "status": "pending",
    "description": "Extend apcKey25::getDebugState() or add to syslog heartbeat: locked status, transpose pitch, pitch wheel offset, drift target, computed ratio. Queryable for troubleshooting.",
    "effort": "small",
    "category": "infra",
    "blocking": [],
    "blockedBy": [
      "wire-transpose-to-wrapper"
    ],
    "acceptance": [
      "Debug endpoint shows transpose state",
      "Live monitoring of lock/unlock, pitch changes, drift",
      "No performance impact"
    ],
    "edge_cases": []
  }
]
